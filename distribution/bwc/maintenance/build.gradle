// 跳过测试
gradle.taskGraph.whenReady {
    tasks.each { task ->
        if (task.name.contains("test")) {
            task.enabled = false
        }

        if (task.name.contains("Test")) {
            task.enabled = false
        }

        if (task.name.contains("buildBwcZip")) {
            task.enabled = false
        }

        if (task.name.contains("checkoutBwcBranch")) {
            task.enabled = false
        }

        if (task.name.contains("fetchLatest")) {
            task.enabled = false
        }

    }
}